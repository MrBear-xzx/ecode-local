# 更新日志

本文件记录用户可感知的版本变化。

## 0.9.0 - 2026-08-12

### 新增与优化

- 完整读取 Ecode 树和只读 App 元数据；源码目录展示文本与资源文件，App 悬停说明展示 ID、发布状态、前置顺序和资源数量。
- 二进制资源支持原始字节拉取、选择性推送、删除、冲突处理、本地回退和跨环境变更集，单文件上限为 100MB。
- 资源替换失败时自动恢复远端旧版本；恢复失败会保留恢复快照路径，避免无提示地留下不完整状态。
- 二进制差异显示本地、基线和远端的路径、大小与 SHA-256，不尝试文本 Diff。
- 普通 App 的发布状态统一以生命周期发布结果为准，系统节点单独使用完整树状态，避免未发布目录显示为未知。
- 旧版同步清单、推送记录、变更集和冲突记录可继续读取，并在后续写入时升级。

## 0.8.1 - 2026-08-08

### 新增与优化

- 跨环境变更集支持文件前置加载、根文件夹加载顺序和发布状态，可与代码混合，也可创建纯生命周期变更集。
- 生命周期操作在回读确认后生成环境级历史；侧边栏按目标路径展示，并支持复选批量删除。
- 应用变更集按“取消发布、文件前置、加载顺序、发布”执行；代码或前置配置失败时阻止后续生命周期操作，并支持幂等重试。
- Agent CLI 增加生命周期历史查询、删除和变更集选择参数；新 schema 兼容旧版代码变更集。
- 修正生命周期写入超时或结果缺失时的状态判断，并通过双环境实测验证前置、顺序和发布迁移。

## 0.8.0 - 2026-08-07

### 新增

- 合并“源码目录”和 Ecode 源码结构；用图标、颜色和简短状态区分分类、可发布根文件夹及前置文件，并支持就地操作。原生资源管理器只装饰同步变更。
- 增加生命周期接口；并发读取发布状态与源码树，使用环境级快照加快展示，写操作独立确认并回读验证。
- 发布状态无法确认时明确显示未知并禁用发布、取消发布入口，避免误判为未发布。
- 通用 Agent CLI、Skill 和 AI 指南同步支持生命周期操作，包含精确路径、加载顺序、特殊 `config*` 文件及无法验证时的保护规则。
- 移除重复列表和无效入口；修正非法 Agent 请求 action 丢失、同目录连接复用旧快照等问题。

## 0.7.1 - 2026-08-05

### 修复与优化

- 删除环境时先隔离源码与状态，配置提交失败会自动恢复；最终清理失败时提示保留位置，避免源码已删但配置仍存在。
- 单个无法安全映射的远端名称不再阻断整个工作区同步，而是单独标记为不支持并继续处理其他文件。
- 环境目录补全 `COM1` 至 `COM9`、`LPT1` 至 `LPT9` 等 Windows 设备名校验。
- 允许同步 `CON.js`、`COM1.js` 等远端设备名文件；部分 Windows Shell 工具可能仍无法稳定寻址，建议在 VS Code 中操作。
- 限制 Agent CLI 已完成请求历史和推送差异虚拟文档缓存，避免长期运行时无界积累；待处理请求不会被历史清理误删。

## 0.7.0 - 2026-08-05

### 新增与调整

- 插件功能统一通过通用 Agent CLI 开放；首次从 Ecode 侧边栏成功配置环境后，自动生成中文 Skill、action 参考和 CLI。
- Agent CLI 自动识别工作区与活动环境，并以结构化 JSON 返回结果；需要 VS Code 表单或选择器的操作改为返回 `pending`，完成交互后通过原请求 ID 续查。
- 增加删除环境功能；删除时同步清理本地源码、状态和凭据，且禁止删除最后一个环境。
- 增加“启用 AI Coding 支持”，用于重新生成已移除的知识、Skill、CLI 和 AGENTS 管理区块。
- 变更集界面统一使用“删除变更集”文案。

### 同步与安全

- “刷新本地与远端变更”现在会重新扫描远端，可识别远端修改、删除和双端冲突，但不会应用远端源码。
- 全量拉取不再显示确认框。
- Agent 执行环境切换、推送、回退、删除、冲突处理、变更集应用或移除 AI 支持前，必须取得当前任务授权并添加 `--confirmed`；VS Code 不再重复确认。
- 修正切换环境后 Agent CLI 返回的活动环境目录。

## 0.6.2 - 2026-08-03

### 修复与优化

- 修复多根工作区取消配置后可能切换到错误上下文的问题，并使切换前未完成的 AI 推送请求失效。
- 拉取取消或异常时仍保存已成功文件的同步清单，避免下次重复处理。
- 优化大规模文件扫描、哈希和同步清单写入，减少重复磁盘操作。
- 工作区清单、推送记录和 AI 结果改为串行原子写入，降低并发写入损坏风险。
- 加强多环境快照、冲突和恢复数据的隔离。

## 0.6.1 - 2026-07-31

### 新增与调整

- 推送记录支持重命名、删除、查看文件差异和单文件本地回退；删除记录不会修改源码或既有变更集。
- 手动推送在选择文件后直接执行；Agent 推送仍需要当前任务的明确授权。
- 应用变更集改为普通确认，并为实际写入文件创建目标环境推送记录和回退快照。

### 修复

- 安全去重远端返回的同 ID 重名节点；无法唯一判断时只隔离歧义文件或目录子树，其他内容继续同步。
- 歧义路径不会被误判为远端删除，也不会被推送或变更集写入；日志提供脱敏后的排查信息。
- 避免重复目录任务、循环引用和潜在死循环。

## 0.6.0 - 2026-07-30

### 破坏性升级

- 0.6 不兼容且不迁移 0.5 及以下项目；检测到旧目录或配置时会阻止运行。请先用旧版本完成同步和备份，再重新配置环境。
- 环境配置迁入 `.ecode-local/environments.json`，每个环境使用独立源码目录和状态目录；公共知识与变更集分别存放在 `common/` 和 `promotion/`。

### 新增与调整

- 多个环境地位相同，可从推送记录创建跨环境变更集，并检查文件和差异。
- 变更集按最终文件快照创建、覆盖或删除目标文件，支持在部分成功后重复应用。
- 推送记录支持文件差异和本地回退；回退不修改远端或同步基线。
- 未完成全量拉取的环境不再误报全部文件，也不能推送或应用变更集。
- 增加路径、文件/目录冲突、远端状态和 GBK 字符校验；推送超时或 5xx 时通过回读判断结果。

## 0.5.0 - 2026-07-28

### 新增与调整

- 增加基于 Ecode 表单元数据的字段补全、悬停说明和跳转，覆盖 `WfForm`、`ModeForm` 的常见字段参数和明细表场景。
- 支持追踪静态 `convertFieldNameToId` 结果，并为变量声明和引用补充字段说明。
- AI Coding 支持新增环境表单元数据，供 Agent 离线读取。
- AI 资料和同步状态统一迁入 `.ecode-local/`；Git 工作区会补充对应忽略规则。
- 表单元数据按服务器与远端路径隔离，仅在成功全量拉取后原子更新；拉取取消或格式异常时保留旧缓存。

### 限制

- 仅识别可静态求值的字段、表单上下文和配置；动态值或冲突值不会被猜测。
- 建模表单是否可获得完整字段信息取决于服务端响应格式。

## 0.4.0 - 2026-07-27

### 新增与调整

- 增加面向通用 Agent 的 Ecode API、组件和项目组件知识，并维护根目录 `AGENTS.md` 入口。
- 增加刷新、打开、移除 AI Coding 支持以及固定源码目录迁移命令。
- Ecode 源码目录固定为工作区根目录下的 `ecode/`；旧自定义目录需要用户确认，不会自动移动、覆盖或删除。
- 语言服务和 AI 支持共享 `setCom/getCom` 组件索引，并在文件变化时增量刷新。
- Ecode 视图标题栏仅保留高频同步操作，配置、文档和 AI 维护命令移入更多操作。
- 不修改业务项目的 `jsconfig.json`、`tsconfig.json` 或 Git 配置；未知类型保守输出为 `unknown`。

## 0.3.0 - 2026-07-26

### 新增

- 为 JavaScript、JSX、TypeScript 和 TSX 增加 Ecode API 成员补全、代码片段、参数签名、悬停和文档跳转。
- 增加 `ecCom` 与 Ecode 内置 `antd` 组件和 props 补全，包括部分常用嵌套参数。
- 增加静态 `ecodeSDK.setCom/getCom` 组件名补全、跨文件定义跳转和引用查找。
- 增加“搜索开发文档”和“打开官方文档”命令。
- 组件索引在后台预热，并随文件变化增量更新。

### 限制

- API 与组件知识可能因 Ecology、KB 或组件版本不同而存在差异。
- `setCom/getCom` 关联仅支持静态字符串形式的 `appId` 和组件名。

## 0.2.3 - 2026-07-24

### 修复

- 修复统一认证刷新 `ecology_JSessionid` 后仍使用旧会话，导致登录成功但远端文件树读取失败的问题。
- 登录过程中持续接收最新 session Cookie，并将最终认证会话保存到 VS Code `SecretStorage`。

## 0.2.2 - 2026-07-24

### 新增与安全

- 支持本地和远端文件删除同步，以及满足安全条件时的目录删除同步。
- 增加本地变更回退和远端删除冲突处理。
- 本地文件变化后的自动刷新延迟由 5 秒缩短为 2 秒。
- 删除前检查远端内容和同步基线；应用远端删除前保存恢复副本，本地存在修改时转为冲突。
- 仅在目录中全部远端文件均可安全删除时删除目录，否则逐文件处理；删除后回读远端验证结果。
